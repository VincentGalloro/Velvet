        Note that "isDown" will return true every frame as long as the key is being held down, whereas "isPressed" will return true on the first frame and only the first frame that the button is pressed